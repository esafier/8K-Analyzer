# Database (local SQLite file, not needed on Render)
filings.db

# Environment variables (never commit secrets)
.env

# Python cache files
__pycache__/
*.pyc

# Virtual environment
venv/

# Cached SEC CIK-to-ticker mapping (auto-downloaded, ~6MB)
cik_tickers_cache.json
